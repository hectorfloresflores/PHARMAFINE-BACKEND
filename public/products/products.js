

document.addEventListener("click", function(event) {
    let xhr = new XMLHttpRequest();
    switch (event.target.id) {
      case "ph_sign_in":
          break;
       default:
           break;
    }
});       

  